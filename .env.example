# The platform runs fully offline for free using a built-in heuristic strategy engine.
# Everything below is OPTIONAL and only needed if you want an LLM to drive attack
# decisions and write the scorecard summary instead of the free engine.
#
# To enable the LLM: set USE_LLM=1 and provide a valid key.
USE_LLM=0
ANTHROPIC_API_KEY=
